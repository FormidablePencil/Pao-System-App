import React from 'react'
import { View, Text } from 'react-native'
import { white } from 'react-native-paper/lib/typescript/src/styles/colors'

const RandomStudyModeOpts = () => {
  return (
    <View>
      <Text style={{ color: 'white', fontSize: 20 }}>
        toggleSudyMode, toggleStaredList, toggle num what Side,

        specify amount of cards cards to study and persist data to paoTables screen but render them all in one list
      </Text>
    </View>
  )
}

export default RandomStudyModeOpts
